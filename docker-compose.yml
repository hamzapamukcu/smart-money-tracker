version: "3.9"

services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data          # persist SQLite across restarts
    env_file:
      - .env
    restart: unless-stopped

  # Uncomment for Phase 2 — run scheduler as a separate worker
  # scheduler:
  #   build: .
  #   command: python -c "from src.scheduler import start_scheduler; import time; start_scheduler(); [time.sleep(60) for _ in iter(int, 1)]"
  #   volumes:
  #     - ./data:/app/data
  #   env_file:
  #     - .env
  #   restart: unless-stopped
  #   depends_on:
  #     - app
